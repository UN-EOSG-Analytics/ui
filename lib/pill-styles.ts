import { typography } from "./typography";

/** Shared shape, spacing, typography, and focus treatment for legend/filter pills. */
export const pillStyles = `${typography.caption} inline-flex items-center gap-1.5 rounded-full border border-border px-2 py-1 text-start text-foreground focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-un-blue`;
